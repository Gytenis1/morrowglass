{
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build && node scripts/generate-static-seo.mjs",
    "check:static-seo": "node scripts/check-static-seo.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "pocketbase": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "4.107.1",
    "@cloudflare/workerd-linux-64": "1.20260702.1",
    "@rolldown/binding-linux-x64-musl": "1.1.4",
    "lightningcss-linux-x64-musl": "1.32.0"
  },
  "name": "pocketbase-frontend",
  "private": true,
  "type": "module"
}
