# The Linux workerd binary is declared directly for deterministic Wrangler deployment.
# Omit optional platform binaries; they are unnecessary on this Linux build host.
omit=optional
# Keep package downloads off the constrained repository volume during CI/deploy.
cache=/dev/shm/npm-cache
